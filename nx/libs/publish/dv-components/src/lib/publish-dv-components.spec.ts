import { publishDvComponents } from './publish-dv-components';

describe('publishDvComponents', () => {
  it('should work', () => {
    expect(publishDvComponents()).toEqual('publish-dv-components');
  });
});
