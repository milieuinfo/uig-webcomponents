import { html } from 'lit-html';
import '../authentication-cookie';

export default {
  title: 'custom-elements/vl-cookie-statement/vl-authentication-cookie',
};

export const Default = () => html`<vl-authentication-cookie></vl-authentication-cookie>`;
