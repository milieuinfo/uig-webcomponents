import { html } from 'lit-html';
import '../jsessionid-cookie';

export default {
  title: 'custom-elements/vl-cookie-statement/vl-jsessionid-cookie',
};

export const Default = () => html`<vl-jsessionid-cookie></vl-jsessionid-cookie>`;
